<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Facebook Profile</title>
  <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet">
  <style>
    body {
      font-family: Arial, sans-serif;
      margin: 0;
      padding: 0;
      background-color: #f0f2f5;
    }

    .profile-container {
      max-width: 600px;
      margin: 50px auto;
      background-color: #fff;
      border-radius: 8px;
      box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
      overflow: hidden;
    }

    .profile-image {
      width: 100%;
      height: 400px;
      background-color: #ddd; /* Default background color */
      display: flex;
      align-items: center;
      justify-content: center;
      cursor: pointer;
    }

    .profile-image img {
      max-width: 100%;
      max-height: 100%;
      /* border-radius: 50%; */
    }

    .profile-info {
      padding: 20px;
    }

    .profile-info h2 {
      margin-bottom: 10px;
    }

    .profile-info form {
      margin-bottom: 20px;
    }

    .profile-info input[type="file"] {
      display: none;
    }

    .profile-info button {
      padding: 8px 16px;
      background-color: #3b5998;
      color: #fff;
      border: none;
      border-radius: 4px;
      cursor: pointer;
    }

    .menu-icon {
      font-size: 24px;
      color: #3b5998;
      cursor: pointer;
      text-align: right;
      margin-top: 10px;
    }
    #back i{
      font-size: 7vw;
      background-color: transparent;
     
    }
    a{
      text-decoration: none;
    }
  </style>
</head>
<body>
  <div class="profile-container">
    <div id="back">
      <a href="/main"><i class="ri-arrow-left-s-line"></i></a>
    </div>
    <div class="profile-image" onclick="document.getElementById('profileImageInput').click();">
        <img id="selectedImage" src="/images/uploads/<%=user.profileImage%>" alt="Profile Picture">
    </div>
    <div class="profile-info">
      <h2><%=user.username%></h2>
      <form id="profileForm" action="/profile" method="post" enctype="multipart/form-data">
        <input type="file" id="profileImageInput" name="image" style="display: none;" onchange="uploadProfile()">
        <button type="submit">Edit Profile</button>
      </form>
      <div class="menu-icon">
        <i class="ri-more-2-fill"></i>
      </div>
    </div>
  </div>

  <script>
    function uploadProfile() {
      document.getElementById("profileForm").submit();
    }
  </script>
</body>
</html>
